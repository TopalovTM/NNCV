Usage of Ruff for linting and formatting.

Locally run:

```
pip install -r requirements-dev.txt
pre-commit install
```
Usefull commands:
```
ruff check .
ruff check . --fix
ruff format .
ruff format --check .
```
